// Note: experimental_wrapLanguageModel is not available in current AI SDK version
// import { experimental_wrapLanguageModel as wrapLanguageModel } from 'ai';

// Middleware for v5 - currently empty but ready for custom logic
export const customMiddleware = {};

// Stub for when experimental_wrapLanguageModel becomes available
export const wrapLanguageModel = undefined;
